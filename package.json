{
  "name": "ai-dlp-backend",
  "version": "1.0.0",
  "description": "AI Landing Page Builder Backend API",
  "scripts": {
    "dev": "uvicorn main:app --reload",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000"
  },
  "engines": {
    "node": "18.x"
  }
}

